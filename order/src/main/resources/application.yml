server:
  port: 9000
  servlet:
    context-path: /

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: zhj@2010[]
    url: jdbc:mysql://192.168.244.128:3306/oms
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      table-prefix: oms_
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl